<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Playlist</title>
</head>
<body>
    <h1>Playlist</h1>
    <ul>
        <% playlist.forEach(item => { %>
            <li>
                <a href="<%= item.url %>"><%= item.title %></a>
                <a href="/download?url=<%= encodeURIComponent(item.url) %>">Download</a>
            </li>
        <% }); %>
    </ul>
</body>
</html>